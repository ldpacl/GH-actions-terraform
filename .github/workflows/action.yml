name: "Terraform Workflow with GitHub Actions"

on:
  pull_request:
    branches:
      - main
    paths:
      - terraform/**
  push:
    branches:
      - main
    paths:
      - terraform/**

permissions:
  contents: read
  issues: write
  pull-requests: write

env:
  TF_LOG: INFO
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  BUCKET_TF_STATE: ${{ secrets.BUCKET_TF_STATE }}
  AWS_REGION: us-east-1

jobs:
  terraform:
    name: "Terraform Workflow"
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ./terraform

    steps:
      - name: Checkout the repo to the runner
        uses: actions/checkout@v4

      - name: Setup Terraform with latest version
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false

      - name: Terraform init
        id: init
        continue-on-error: ${{ github.event_name == 'pull_request' }}
        run: |
          set -o pipefail
          LOG_FILE="$RUNNER_TEMP/init.log"
          terraform init -backend-config="bucket=$BUCKET_TF_STATE" 2>&1 | tee "$LOG_FILE"
          EXIT=${PIPESTATUS[0]}
          if [ "$(stat -c%s "$LOG_FILE")" -gt 12000 ]; then
            printf '%s\n' "... (log truncated, showing last 10KB) ..." >"${LOG_FILE}.new"
            tail -c 10000 "$LOG_FILE" >>"${LOG_FILE}.new"
            mv "${LOG_FILE}.new" "$LOG_FILE"
          fi
          delim="INITLOG_${GITHUB_RUN_ID}_${RANDOM}_EOF"
          {
            echo "log<<$delim"
            cat "$LOG_FILE"
            echo "$delim"
          } >>"$GITHUB_OUTPUT"
          exit "$EXIT"

      - name: Terraform format
        id: format
        continue-on-error: ${{ github.event_name == 'pull_request' }}
        run: |
          set -o pipefail
          LOG_FILE="$RUNNER_TEMP/format.log"
          terraform fmt -check 2>&1 | tee "$LOG_FILE"
          EXIT=${PIPESTATUS[0]}
          if [ "$(stat -c%s "$LOG_FILE")" -gt 12000 ]; then
            printf '%s\n' "... (log truncated, showing last 10KB) ..." >"${LOG_FILE}.new"
            tail -c 10000 "$LOG_FILE" >>"${LOG_FILE}.new"
            mv "${LOG_FILE}.new" "$LOG_FILE"
          fi
          delim="FMTLOG_${GITHUB_RUN_ID}_${RANDOM}_EOF"
          {
            echo "log<<$delim"
            cat "$LOG_FILE"
            echo "$delim"
          } >>"$GITHUB_OUTPUT"
          exit "$EXIT"

      - name: Terraform validate
        id: validate
        continue-on-error: ${{ github.event_name == 'pull_request' }}
        run: |
          set -o pipefail
          LOG_FILE="$RUNNER_TEMP/validate.log"
          terraform validate 2>&1 | tee "$LOG_FILE"
          EXIT=${PIPESTATUS[0]}
          if [ "$(stat -c%s "$LOG_FILE")" -gt 12000 ]; then
            printf '%s\n' "... (log truncated, showing last 10KB) ..." >"${LOG_FILE}.new"
            tail -c 10000 "$LOG_FILE" >>"${LOG_FILE}.new"
            mv "${LOG_FILE}.new" "$LOG_FILE"
          fi
          delim="VALLOG_${GITHUB_RUN_ID}_${RANDOM}_EOF"
          {
            echo "log<<$delim"
            cat "$LOG_FILE"
            echo "$delim"
          } >>"$GITHUB_OUTPUT"
          exit "$EXIT"

      - name: Terraform plan
        id: plan
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          set -o pipefail
          LOG_FILE="$RUNNER_TEMP/plan.log"
          terraform plan -no-color -input=false 2>&1 | tee "$LOG_FILE"
          EXIT=${PIPESTATUS[0]}
          if [ "$(stat -c%s "$LOG_FILE")" -gt 12000 ]; then
            printf '%s\n' "... (log truncated, showing last 10KB) ..." >"${LOG_FILE}.new"
            tail -c 10000 "$LOG_FILE" >>"${LOG_FILE}.new"
            mv "${LOG_FILE}.new" "$LOG_FILE"
          fi
          delim="PLANLOG_${GITHUB_RUN_ID}_${RANDOM}_EOF"
          {
            echo "log<<$delim"
            cat "$LOG_FILE"
            echo "$delim"
          } >>"$GITHUB_OUTPUT"
          exit "$EXIT"

      - uses: actions/github-script@v7
        if: always() && github.event_name == 'pull_request'
        env:
          INIT_OUTCOME: ${{ steps.init.outcome }}
          FORMAT_OUTCOME: ${{ steps.format.outcome }}
          VALIDATE_OUTCOME: ${{ steps.validate.outcome }}
          PLAN_OUTCOME: ${{ steps.plan.outcome }}
          INIT_LOG: ${{ steps.init.outputs.log }}
          FORMAT_LOG: ${{ steps.format.outputs.log }}
          VALIDATE_LOG: ${{ steps.validate.outputs.log }}
          PLAN_LOG: ${{ steps.plan.outputs.log }}
        with:
          script: |
            const marker = '<!-- gh-actions-terraform-ci-report -->';

            function escFence(s) {
              // Break accidental ``` sequences so they do not terminate the markdown fence.
              return String(s || '_No output captured._').replace(/```/g, '`\u200b``');
            }

            function section(title, outcome, log) {
              const icon =
                outcome === 'success'
                  ? '✅'
                  : outcome === 'failure'
                    ? '❌'
                    : outcome === 'skipped'
                      ? '⏭️'
                      : '❔';
              const reason =
                outcome === 'failure'
                  ? 'This step failed; see output below for the Terraform message.'
                  : outcome === 'skipped'
                    ? 'This step did not run for this event.'
                    : 'Completed without errors (exit code 0).';
              return [
                `### ${icon} ${title}`,
                '',
                `**Result:** \`${outcome}\` — ${reason}`,
                '',
                '<details><summary>Command output</summary>',
                '',
                '```text',
                escFence(log),
                '```',
                '',
                '</details>',
              ].join('\n');
            }

            const outcomes = [
              process.env.INIT_OUTCOME,
              process.env.FORMAT_OUTCOME,
              process.env.VALIDATE_OUTCOME,
              process.env.PLAN_OUTCOME,
            ];
            const summaryLine = outcomes.some((o) => o === 'failure')
              ? '**Overall:** at least one check failed. Fix the issues below before merging.'
              : '**Overall:** all executed checks passed.';

            const body = [
              marker,
              '## Terraform CI report',
              '',
              summaryLine,
              '',
              section('Terraform init', process.env.INIT_OUTCOME, process.env.INIT_LOG),
              '',
              section('Terraform format (`fmt -check`)', process.env.FORMAT_OUTCOME, process.env.FORMAT_LOG),
              '',
              section('Terraform validate', process.env.VALIDATE_OUTCOME, process.env.VALIDATE_LOG),
              '',
              section('Terraform plan', process.env.PLAN_OUTCOME, process.env.PLAN_LOG),
              '',
              `*Workflow: \`${process.env.GITHUB_RUN_ID}\` · Actor: @${process.env.GITHUB_ACTOR}*`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });

            const existing = comments.find((c) => c.body?.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

      - name: Fail PR if any Terraform check failed
        if: >-
          always() &&
          github.event_name == 'pull_request' &&
          (steps.init.outcome == 'failure' ||
           steps.format.outcome == 'failure' ||
           steps.validate.outcome == 'failure' ||
           steps.plan.outcome == 'failure')
        run: exit 1

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false
